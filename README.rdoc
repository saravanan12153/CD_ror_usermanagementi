User Management System I
Create a new model called User with the following fields:

first_name
last_name
email_address
password
Create necessary models. Using the console create a few user records.

Do not use resources for the routes.

Now using rails, in the Users controller index method...

Get all the users records
Pass the users information to the appropriate view file
Use an each loop to display all of the user's information (first name, last name, and email address)
